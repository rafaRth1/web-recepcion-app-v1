services:
   frontend:
      build:
         context: .
         dockerfile: deploy/Dockerfile
      container_name: ezechis-frontend
      restart: always
      ports:
         - "3000:3000"
      environment:
         - NODE_ENV=production
